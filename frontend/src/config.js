// Configuration file for API base URL
// When running in Capacitor (native app), we MUST use the PC's explicit local IP.
// When running in Vite Dev Server, we can use the relative path (proxy).

const isNative = !!window.Capacitor;
export const API_BASE_URL = 'https://harmony-1d8i.onrender.com';
